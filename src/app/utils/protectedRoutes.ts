export const protectedRoutes = new Set([
    "/dashboard",
    "/profile",
    "/settings"
]);
export const protectedRoutesAdmin = new Set([
    "/admin",
    "/admin/users",
    "/admin/settings"
]);
